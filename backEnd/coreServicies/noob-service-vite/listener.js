export default class listener{
    constructor(){}
    startListen(){
       /*Object.getOwnPropertyNames(console).forEach(
            (name)=>{
                let fn= console[name]
                console[name]=(...args)=>{
                    try{
                    this.mainBridge.call('noob-service-vite','console',...args)
                    }catch(e){
                        
                    }
                    fn(...args)
                }
            }
        )*/
       
    }
}